// Fórmula de báskhara, que serve para calcular as raízes de uma equação de segundo grau.

// Calcular o valor de delta, a partir dos coeficientes A, B e C

let a = 4 
let b = 9
let c = 5

let delta = Math.pow(b,2)  - (4 * a * c)

console.log(delta)